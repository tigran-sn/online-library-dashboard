import { InitialsAvatarDirective } from './initials-avatar.directive';

describe('InitialsAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new InitialsAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
